pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh '''
                    ls -al
                    node --version
                    npm --version
                    cd frontend-marketing && npm ci && npm run build
                    ls -al
                '''
            }
        }
    }
}